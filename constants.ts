// This is your Google Tag Manager (GTM) Container ID.
// All other tracking tags (Google Analytics, Google Ads, Facebook Pixel, etc.)
// should be configured inside your GTM container on the GTM website.
export const GTM_ID = 'GTM-N7LN5XBL';

/**
 * These are example event names for conversion tracking.
 * You will use these names to create "Custom Event" triggers in GTM.
 * The actual Conversion ID and Label from Google Ads will be configured
 * inside the Google Ads Conversion Tracking tag you create in GTM.
 */
export const GTM_CONTACT_FORM_EVENT = 'generate_lead_contact';
export const GTM_WIZARD_FORM_EVENT = 'generate_lead_wizard';
export const GTM_CLICK_TO_CALL_EVENT = 'click_to_call';

// --- PPL Campaign Events from Marketing Plan ---
export const GTM_VIEW_LANDING_PAGE_EVENT = 'view_landing_page';
export const GTM_GENERATE_LEAD_EVENT = 'generate_lead';